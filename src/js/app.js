// Initialize map and event listeners

import { initMobileNav } from './components/navigation.js';

document.addEventListener('DOMContentLoaded', function () {
	console.log('Netflify test');

	initMobileNav();
});
